{
  "name": "@tarstate/demo",
  "private": true,
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "vite build --config ../../vite.config.ts --logLevel warn",
    "dev": "vite --config ../../vite.config.ts",
    "preview": "vite preview --config ../../vite.config.ts --host 127.0.0.1",
    "test": "vitest run --config ../../vite.config.ts --root .",
    "typecheck": "tsc -p ../../tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tarstate/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
