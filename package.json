{
  "name": "tarstate",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "bench": "pnpm --filter './packages/*' --if-present bench",
    "bench:legacy": "node scripts/legacy-benchmark.mjs",
    "build": "pnpm --filter './packages/*' -r build",
    "check": "pnpm lint && pnpm typecheck && pnpm typecheck:budget && pnpm build && pnpm test",
    "check:release": "pnpm check && pnpm bench && tsc -p tsconfig.release.json && node scripts/verify-release.mjs",
    "lint": "oxlint . --deny-warnings --type-aware",
    "test": "pnpm --filter './packages/*' test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:budget": "pnpm --filter @tarstate/core typecheck:budget"
  },
  "devDependencies": {
    "@typescript/native": "catalog:",
    "@types/node": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
